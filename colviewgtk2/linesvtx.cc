

  // glBegin(GL_LINES);
  // if(d->BackGround < 0.5) glColor3f(1.0, 1.0, 1.0);
  // else                    glColor3f(0.0, 0.0, 0.0);

  // glColor3f(1.0, 1.0, 1.0);
  // for(unsigned int i=0; i<lines.size(); i++) {
  //   glVertex3f(lines[i].x, lines[i].y, lines[i].z -1);
  // }
  // glEnd();
